{{>navbar}}
    <h1>{{title}}</h1>
    
    {{#each productos}}
        <div>
            <h2>{{this.title}}</h2>
            <img src="{{this.thumbnail}}">
            <p>{{this.price}}</p>
            <h3>{{this.id}}</h3>
        </div>
    {{/each}}

    {{>footer}}
